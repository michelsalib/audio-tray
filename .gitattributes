# Normalize text files to LF in the repo. Keeps workflow YAML and the bash step
# in release.yml free of CRLF, which would otherwise break as $'\r' on runners.
* text=auto eol=lf

# Binary assets — never touch line endings.
*.png binary
*.ico binary
