# Rust build output (one workspace target dir covers every member)
/target

# Backup / editor files
*.bak
*~

# Packaging build output (installer + zipped binary produced by CI or local runs)
/packaging/dist
/dist

# OS junk
Thumbs.db
Desktop.ini
