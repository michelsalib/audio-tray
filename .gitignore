# Rust build output. The spike keeps its own workspace, so it has its own target.
/target
/spikes/*/target

# Backup / editor files
*.bak
*~

# Packaging build output (installer + zipped binary produced by CI or local runs)
/packaging/dist
/dist

# OS junk
Thumbs.db
Desktop.ini
